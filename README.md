# TinyTinyRSS Updater Container

This is a sidecar container for `paulritter/tinytinyrss` that updates the feeds. Mount your `config.php` from the main container to `/usr/src/ttrss` in this container.
